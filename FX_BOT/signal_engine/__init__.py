"""Signal engine: pure, deterministic SMC setup detection as a state machine.

Public entry point: ``signal_engine.setup.find_setup`` — the ONLY strategy hook
used by both the backtester and the live scanner.
"""
